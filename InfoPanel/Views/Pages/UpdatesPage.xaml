<Page
    x:Class="InfoPanel.Views.Pages.UpdatesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="using:InfoPanel"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <ScrollViewer>
        <StackPanel MaxWidth="1360" Margin="20,20,20,20" Spacing="10">
            <!-- Current Version Card -->
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="16">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <SymbolIcon Symbol="Sync" Grid.Column="0" Margin="0,0,12,0" VerticalAlignment="Top" />
                    <StackPanel Grid.Column="1">
                        <TextBlock x:Name="TextBlockVersion" FontSize="13" FontWeight="SemiBold" />
                        <TextBlock x:Name="TextBlockUpdateStatus" Margin="0,5,0,0" FontSize="12" Opacity="0.7" Text="Checking for updates..." />
                    </StackPanel>
                    <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="10">
                        <Button x:Name="ButtonUpdate" Content="Update" Style="{ThemeResource AccentButtonStyle}" Click="ButtonUpdate_Click" Visibility="Collapsed" />
                        <ProgressBar x:Name="ProgressBarDownload" Width="150" Visibility="Collapsed" />
                    </StackPanel>
                    <Button x:Name="ButtonCheckUpdates" Grid.Column="3" Content="Check for updates" Click="ButtonCheckUpdates_Click" Margin="10,0,0,0" />
                </Grid>
            </Border>

            <!-- What's New Section -->
            <TextBlock Text="What's New" FontSize="14" FontWeight="SemiBold" Margin="0,20,0,10" />
            
            <ItemsRepeater x:Name="RepeaterVersions">
                <ItemsRepeater.Layout>
                    <StackLayout Spacing="10" />
                </ItemsRepeater.Layout>
                <ItemsRepeater.ItemTemplate>
                    <DataTemplate>
                        <Expander HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch" IsExpanded="{Binding Expanded}">
                            <Expander.Header>
                                <StackPanel>
                                    <TextBlock FontSize="13" FontWeight="SemiBold" Text="{Binding Version}" />
                                    <TextBlock FontSize="12" Opacity="0.7" Text="{Binding Title}" />
                                </StackPanel>
                            </Expander.Header>
                            <ItemsRepeater ItemsSource="{Binding Items}">
                                <ItemsRepeater.Layout>
                                    <StackLayout Spacing="10" />
                                </ItemsRepeater.Layout>
                                <ItemsRepeater.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel Margin="0,0,0,10">
                                            <TextBlock FontSize="13" FontWeight="SemiBold" Text="{Binding Title}" />
                                            <ItemsRepeater ItemsSource="{Binding Description}">
                                                <ItemsRepeater.Layout>
                                                    <StackLayout Spacing="2" />
                                                </ItemsRepeater.Layout>
                                                <ItemsRepeater.ItemTemplate>
                                                    <DataTemplate>
                                                        <TextBlock FontSize="12" Opacity="0.7" TextWrapping="Wrap">
                                                            <Run Text="- " />
                                                            <Run Text="{Binding}" />
                                                        </TextBlock>
                                                    </DataTemplate>
                                                </ItemsRepeater.ItemTemplate>
                                            </ItemsRepeater>
                                        </StackPanel>
                                    </DataTemplate>
                                </ItemsRepeater.ItemTemplate>
                            </ItemsRepeater>
                        </Expander>
                    </DataTemplate>
                </ItemsRepeater.ItemTemplate>
            </ItemsRepeater>
        </StackPanel>
    </ScrollViewer>
</Page>
