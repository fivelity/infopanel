<Page
    x:Class="InfoPanel.Views.Pages.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="using:InfoPanel"
    d:DesignHeight="900"
    d:DesignWidth="800"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <ScrollViewer>
        <StackPanel MaxWidth="1340" Margin="20,20,20,20" Spacing="10">

            <!-- Application Section -->
            <TextBlock Text="Application" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,10" />

            <!-- UI Scale -->
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="16">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <SymbolIcon Symbol="ZoomIn" Grid.Column="0" Margin="0,0,12,0" />
                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock FontSize="13" FontWeight="SemiBold" Text="UI Scale" />
                        <TextBlock FontSize="12" Opacity="0.7" Text="Change InfoPanel UI scale to fit or zoom accordingly." />
                    </StackPanel>
                    <ComboBox x:Name="ComboBoxScale" Grid.Column="2" Width="150" DisplayMemberPath="Display" SelectedValuePath="Value" />
                </Grid>
            </Border>

            <!-- Run on Startup -->
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="16">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <SymbolIcon Symbol="Accept" Grid.Column="0" Margin="0,0,12,0" />
                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock FontSize="13" FontWeight="SemiBold" Text="Run on startup" />
                        <TextBlock FontSize="12" Opacity="0.7" Text="Launch InfoPanel automatically on windows sign in." />
                    </StackPanel>
                    <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="10">
                        <NumberBox x:Name="NumberBoxStartupDelay" Width="120" Header="Delay (secs)" Minimum="0" SpinButtonPlacementMode="Compact" />
                        <ToggleSwitch x:Name="ToggleAutoStart" />
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Minimize to Tray -->
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="16">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <SymbolIcon Symbol="Download" Grid.Column="0" Margin="0,0,12,0" />
                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock FontSize="13" FontWeight="SemiBold" Text="Minimize to tray" />
                        <TextBlock FontSize="12" Opacity="0.7" Text="Minimize InfoPanel to the system tray instead." />
                    </StackPanel>
                    <ToggleSwitch x:Name="ToggleMinimizeToTray" Grid.Column="2" />
                </Grid>
            </Border>

            <!-- Start Minimized -->
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="16">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <SymbolIcon Symbol="Remove" Grid.Column="0" Margin="0,0,12,0" />
                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock FontSize="13" FontWeight="SemiBold" Text="Start minimised" />
                        <TextBlock FontSize="12" Opacity="0.7" Text="Runs InfoPanel minimized on startup." />
                    </StackPanel>
                    <ToggleSwitch x:Name="ToggleStartMinimized" Grid.Column="2" />
                </Grid>
            </Border>

            <!-- Panel Section -->
            <TextBlock Text="Panel" FontSize="14" FontWeight="SemiBold" Margin="0,30,0,10" />

            <!-- Selected Item Color -->
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="16">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <SymbolIcon Symbol="SelectAll" Grid.Column="0" Margin="0,0,12,0" />
                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock FontSize="13" FontWeight="SemiBold" Text="Selected item color" />
                        <TextBlock FontSize="12" Opacity="0.7" Text="Select an item via the Design Page or middle click on the panel itself." />
                    </StackPanel>
                    <ColorPicker x:Name="ColorPickerSelectedItem" Grid.Column="2" ColorSpectrumShape="Ring" IsMoreButtonVisible="False" IsColorSliderVisible="True" IsColorChannelTextInputVisible="False" IsHexInputVisible="True" />
                </Grid>
            </Border>

            <!-- Show Grid Lines -->
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="16">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <SymbolIcon Symbol="ViewAll" Grid.Column="0" Margin="0,0,12,0" />
                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock FontSize="13" FontWeight="SemiBold" Text="Show grid lines" />
                        <TextBlock FontSize="12" Opacity="0.7" Text="Display grid lines when an item is selected." />
                    </StackPanel>
                    <ToggleSwitch x:Name="ToggleShowGridLines" Grid.Column="2" />
                </Grid>
            </Border>

            <!-- Grid Lines Settings -->
            <Border x:Name="BorderGridSettings" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="16">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <SymbolIcon Symbol="ViewAll" Grid.Column="0" Margin="0,0,12,0" />
                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock FontSize="13" FontWeight="SemiBold" Text="Grid lines settings" />
                        <TextBlock FontSize="12" Opacity="0.7" Text="Customise the grid lines to your preferences." />
                    </StackPanel>
                    <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="10">
                        <ColorPicker x:Name="ColorPickerGridLines" ColorSpectrumShape="Ring" IsMoreButtonVisible="False" IsColorSliderVisible="True" IsColorChannelTextInputVisible="False" IsHexInputVisible="True" />
                        <NumberBox x:Name="NumberBoxGridSpacing" Width="120" Header="Spacing (px)" Minimum="5" SpinButtonPlacementMode="Compact" />
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Libre Section -->
            <TextBlock Text="Libre" FontSize="14" FontWeight="SemiBold" Margin="0,30,0,10" />

            <!-- Enable LibreHardwareMonitor -->
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="16">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <SymbolIcon Symbol="MoveToFolder" Grid.Column="0" Margin="0,0,12,0" />
                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock FontSize="13" FontWeight="SemiBold" Text="Enable LibreHardwareMonitor" />
                        <TextBlock FontSize="12" Opacity="0.7" Text="Enables polling LibreHardwareMonitor sensor data." />
                    </StackPanel>
                    <ToggleSwitch x:Name="ToggleLibreHardwareMonitor" Grid.Column="2" />
                </Grid>
            </Border>

            <!-- PawniO Driver -->
            <Border x:Name="BorderPawnIO" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="16">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <SymbolIcon Symbol="Repair" Grid.Column="0" Margin="0,0,12,0" />
                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock FontSize="13" FontWeight="SemiBold" Text="PawniO Driver" />
                        <TextBlock FontSize="12" Opacity="0.7" Text="Required for low-level hardware access (CPU temps, voltages, etc.)." />
                    </StackPanel>
                    <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="10">
                        <TextBlock x:Name="TextBlockPawnIOStatus" VerticalAlignment="Center" FontSize="12" Opacity="0.7" />
                        <Button x:Name="ButtonCheckPawnIO" Content="Check Status" Click="ButtonCheckPawnIO_Click" />
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Performance Section -->
            <TextBlock Text="Performance" FontSize="14" FontWeight="SemiBold" Margin="0,30,0,10" />

            <!-- Target Frame Rate -->
            <Expander HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch">
                <Expander.Header>
                    <StackPanel Orientation="Horizontal" Spacing="12">
                        <SymbolIcon Symbol="Play" />
                        <StackPanel>
                            <TextBlock FontSize="13" FontWeight="SemiBold" Text="Target Frame Rate" />
                            <TextBlock FontSize="12" Opacity="0.7" Text="The rate at which panels are rendered." />
                        </StackPanel>
                    </StackPanel>
                </Expander.Header>
                <Grid Padding="0,10,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Margin="0,0,10,0">
                        <Slider x:Name="SliderFrameRate" Minimum="1" Maximum="60" TickFrequency="1" SnapsTo="Ticks" />
                        <TextBlock FontSize="12" Opacity="0.7">
                            <Run Text="Target framerate: " />
                            <Run x:Name="RunFrameRate" Text="30" />
                        </TextBlock>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Margin="10,0,0,0">
                        <Slider x:Name="SliderGraphUpdateRate" Minimum="100" Maximum="3000" TickFrequency="100" SnapsTo="Ticks" />
                        <TextBlock FontSize="12" Opacity="0.7">
                            <Run Text="Graph update rate (ms): " />
                            <Run x:Name="RunGraphUpdateRate" Text="500" />
                        </TextBlock>
                    </StackPanel>
                </Grid>
            </Expander>

            <!-- Remote Sensor Section -->
            <TextBlock Text="Remote Sensor" FontSize="14" FontWeight="SemiBold" Margin="0,30,0,10" />

            <!-- Enable Webserver -->
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="16">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <SymbolIcon Symbol="World" Grid.Column="0" Margin="0,0,12,0" />
                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock FontSize="13" FontWeight="SemiBold" Text="Enable webserver" />
                        <TextBlock FontSize="12" Opacity="0.7" Text="Act as a webserver to display panels remotely via HTTP." />
                    </StackPanel>
                    <ToggleSwitch x:Name="ToggleWebServer" Grid.Column="2" />
                </Grid>
            </Border>

            <!-- Listen IP -->
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="16">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <SymbolIcon Symbol="Globe" Grid.Column="0" Margin="0,0,12,0" />
                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock FontSize="13" FontWeight="SemiBold" Text="Listen IP" />
                        <TextBlock FontSize="12" Opacity="0.7" Text="IP to run the webserver on." />
                    </StackPanel>
                    <ComboBox x:Name="ComboBoxListenIp" Grid.Column="2" Width="150" />
                </Grid>
            </Border>

            <!-- Listen Port -->
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="16">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <SymbolIcon Symbol="Globe" Grid.Column="0" Margin="0,0,12,0" />
                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock FontSize="13" FontWeight="SemiBold" Text="Listen Port" />
                        <TextBlock FontSize="12" Opacity="0.7" Text="Port to run the webserver on." />
                    </StackPanel>
                    <ComboBox x:Name="ComboBoxListenPort" Grid.Column="2" Width="150" />
                </Grid>
            </Border>

            <!-- Refresh Rate -->
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="16">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <SymbolIcon Symbol="Sync" Grid.Column="0" Margin="0,0,12,0" />
                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock FontSize="13" FontWeight="SemiBold" Text="Refresh Rate" />
                        <TextBlock FontSize="12" Opacity="0.7" Text="The target rate at which remote sensor updates in milliseconds." />
                    </StackPanel>
                    <ComboBox x:Name="ComboBoxRefreshRate" Grid.Column="2" Width="150" />
                </Grid>
            </Border>

            <!-- Misc Section -->
            <TextBlock Text="Misc" FontSize="14" FontWeight="SemiBold" Margin="0,30,0,10" />

            <!-- Application Data -->
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="16">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <SymbolIcon Symbol="Folder" Grid.Column="0" Margin="0,0,12,0" />
                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock FontSize="13" FontWeight="SemiBold" Text="Application Data" />
                        <TextBlock FontSize="12" Opacity="0.7" Text="Go to where all your data is stored. Backing up often is always a good thing." />
                    </StackPanel>
                    <Button x:Name="ButtonOpenDataFolder" Grid.Column="2" Content="Open Folder" Click="ButtonOpenDataFolder_Click" />
                </Grid>
            </Border>

        </StackPanel>
    </ScrollViewer>
</Page>
