<Window x:Class="InfoPanel.Views.Components.WebServer.WebServerSettings"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="using:InfoPanel.Views.Components.WebServer"
             xmlns:app="using:InfoPanel"
        xmlns:models="using:InfoPanel.Models"
        xmlns:components="using:InfoPanel.Views.Components"
        mc:Ignorable="d"
        Title="WebServerSettings" Width="400" Height="300">
    <Grid Margin="10,10,10,10">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" FontSize="20" Text="WebServer Settings" FontWeight="Medium" Margin="0,0,0,10"/>

        <CheckBox Grid.Row="1" Content="Enable WebServer" IsChecked="{Binding Source={x:Static app:ConfigModel.Instance}, Path=Settings.WebServer}"/>

        <Grid Grid.Row="2" IsEnabled="{Binding Source={x:Static app:ConfigModel.Instance}, Path=Settings.WebServer, Converter={StaticResource InverseBooleanConverter}}" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>
            <ComboBox Name="ComboBoxListenIp" Grid.Column="0"  SelectedValue="{Binding Source={x:Static app:ConfigModel.Instance}, Path=Settings.WebServerListenIp}"/>

            <NumberBox Grid.Column="1" Height="30" Margin="20,0,0,25" 
                   Background="Transparent" BorderThickness="0,0,0,1"
                   FontSize="14"
                   Minimum="80" Maximum="65000" SmallChange="1000"
                   Value="{Binding Source={x:Static app:ConfigModel.Instance}, Path=Settings.WebServerListenPort}" />

        </Grid>
        <Border Grid.Row="3" Visibility="{Binding Source={x:Static app:ConfigModel.Instance}, Path=Settings.WebServer, Converter={StaticResource BooleanToVisibilityConverter}}">
            <TextBlock HorizontalAlignment="Center"
                       Text="{Binding Source={x:Static app:ConfigModel.Instance}, Path=Settings.WebServerListenIp, StringFormat='Listening on http://{0}', Mode=OneWay}" />
        </Border>

        <ItemsControl Grid.Row="4" ItemsSource="{Binding Source={x:Static app:ConfigModel.Instance}, Path=Profiles}" 
                      Visibility="{Binding Source={x:Static app:ConfigModel.Instance}, Path=Settings.WebServer, Converter={StaticResource BooleanToVisibilityConverter}}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Grid.Row="3">
                        <TextBlock HorizontalAlignment="Center"
                                   Text="{Binding Name, StringFormat='{0}', Mode=OneWay}" />
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>


      
    </Grid>
</Window>
