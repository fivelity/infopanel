<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!--
    ========================================
    SEPARATOR COMPONENT
    Visual dividers between sections
    ========================================
    -->

    <!-- Horizontal Separator -->
    <Style x:Key="DS_Separator" TargetType="Separator">
        <Setter Property="Background" Value="{StaticResource DS_BorderBrush}"/>
        <Setter Property="Height" Value="1"/>
        <Setter Property="Margin" Value="0,16,0,16"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Separator">
                    <Rectangle Fill="{TemplateBinding Background}"
                             Height="{TemplateBinding Height}"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Vertical Separator -->
    <Style x:Key="DS_Separator_Vertical" TargetType="Separator" BasedOn="{StaticResource DS_Separator}">
        <Setter Property="Width" Value="1"/>
        <Setter Property="Height" Value="Auto"/>
        <Setter Property="Margin" Value="16,0,16,0"/>
    </Style>

    <!-- Thin Separator -->
    <Style x:Key="DS_Separator_Sm" TargetType="Separator" BasedOn="{StaticResource DS_Separator}">
        <Setter Property="Margin" Value="0,8,0,8"/>
    </Style>

    <!-- Thick Separator with more spacing -->
    <Style x:Key="DS_Separator_Lg" TargetType="Separator" BasedOn="{StaticResource DS_Separator}">
        <Setter Property="Margin" Value="0,24,0,24"/>
    </Style>

</ResourceDictionary>
